require 'spec_helper'

describe ApplicationController do
  describe "Rating" do
    xit "allows a user to rate a poem" do

    end

    xit "only allows one rating per poem per user" do

    end

    xit "allows a user to update their rating" do

    end
  end
end
